#!/usr/bin/env python

def distance_from_zero(n):
    if type(n) is int or type(n) is float :      
        print(abs(n))        
        return abs(n)
    else:
        print("This isn't an integer or a float!")
        return "This isn't an integer or a float!"
    	
distance_from_zero(fgd)
