def hotelCost(days):
	return 140*days

bill = hotelCost(5)

def getMin(balance, rate):
	print( balance / 100 * rate )
	return balance / 100 * rate
        def addMonthlyInterest(balance):
            balance = balance + balance * 0.15
            return balance + balance * 0.15

getMin(1000, 2)
